
<main>
<body>
<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
<% if @book.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <div class="error_headline"><%= @book.errors.count %>errors prohibited this obj from being saved:<br></div>
      <ul class="error_text">
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<div class="field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class="field">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>
<div class="action">
  <%= f.submit %>
  </div>
<% end %>
</body>
<%= link_to "show", book_path(@book) %>
<%= link_to "Back", books_path %>

</main>